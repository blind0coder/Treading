  GNU nano 8.2       src/main/resources/application.properties
spring.application.name=trading
server.port=5454

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/yt_trading
spring.datasource.username=root
spring.datasource.password=shirish
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.show-sql: true
#logging.level.org.springframework=DEBUG
# Application name and server port
spring.application.name=trading
server.port=5454

# === Database Configurations (from environment variables) ===
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Explicitly set Hibernate dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA behavior settings
spring.jpa.hibernate.ddl-auto=update

# === Payment API Keys (from environment variables) ===
stripe.api.key=${STRIPE_API_KEY}
razorpay.api.key=${RAZORPAY_API_KEY}
razorpay.api.secret=${RAZORPAY_API_SECRET}

